export interface IPlayer {
  playerName: string;
  playerInfo: string;
  sportType: string;
  country: string;
  isActivePlayer: boolean;
  playerId:string
}

export interface IPlayerRes{
  name: string;
}